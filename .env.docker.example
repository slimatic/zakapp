# ZakApp Docker Environment Configuration
# Copy this file to .env.docker and customize as needed
# =========================================

# =====================================================
# PORT CONFIGURATION
# =====================================================
# Backend API Server Port (exposed on host)
# Default: 3001
# The backend will always listen on this port inside the container
BACKEND_PORT=3001

# Frontend Development Server Port (exposed on host)
# Default: 3000
FRONTEND_PORT=3000

# =====================================================
# SECURITY SECRETS (REQUIRED)
# =====================================================
# Generate these with: openssl rand -hex 32
# IMPORTANT: Use unique values for production!

# JWT Authentication Secret
JWT_SECRET=REPLACE_WITH_SECURE_SECRET

# JWT Refresh Token Secret
JWT_REFRESH_SECRET=REPLACE_WITH_SECURE_SECRET

# Data Encryption Key (AES-256)
# Generate with: openssl rand -hex 16
ENCRYPTION_KEY=REPLACE_WITH_SECURE_SECRET

# =====================================================
# DEPLOYMENT CONFIGURATION (OPTIONAL)
# =====================================================
# For production/remote deployments with custom domains:
# Uncomment and set these to override localhost defaults

# Frontend API endpoint (where React app sends requests)
# REACT_APP_API_BASE_URL=https://api.yourdomain.com/api

# Backend CORS allowed origins (where browsers access frontend)
# ALLOWED_ORIGINS=https://yourdomain.com

# =====================================================
# FEATURE FLAGS (OPTIONAL)
# =====================================================
# Enable/disable feedback widget for beta testing
# Set to 'true' to show feedback bubble, 'false' to hide
# Default: true (enabled for development)
# REACT_APP_FEEDBACK_ENABLED=true

# =====================================================
# OPTIONAL CONFIGURATION
# =====================================================
# Metals API Key for live gold/silver prices
# Sign up at: https://metalpriceapi.com/
# METALS_API_KEY=your_api_key_here

# Host IP for mobile/network access
# Automatically detected, but can be overridden
# HOST_IP=192.168.1.100

# =====================================================
# QUICK START
# =====================================================
# 1. Copy this file:
#    cp .env.docker.example .env.docker
#
# 2. Generate secure secrets:
#    ./scripts/docker-start.sh
#    (This will auto-generate secrets if not set)
#
# 3. To change ports:
#    - Edit BACKEND_PORT and FRONTEND_PORT above
#    - Run: docker compose down && docker compose up -d
#
# 4. Access the application:
#    - Frontend: http://localhost:${FRONTEND_PORT}
#    - Backend: http://localhost:${BACKEND_PORT}
#
# =====================================================
# PORT CONFLICT RESOLUTION
# =====================================================
# If you get "port already in use" errors:
#
# 1. Check which process is using the port:
#    lsof -i :3000  # or :3001
#    netstat -an | grep 3000
#
# 2. Change the ports in this file:
#    FRONTEND_PORT=3010
#    BACKEND_PORT=3011
#
# 3. Restart Docker services:
#    docker compose down
#    docker compose up -d
#
# The application will automatically adapt to the new ports!
