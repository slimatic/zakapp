# Root Environment Configuration
# This file serves as a template for environment variables at the root level
# 
# NOTE: This is for workspace-level configuration only.
# For application-specific configuration:
# - Backend: See server/.env.example
# - Frontend: See client/.env.example

# Development Environment
NODE_ENV=development

# Database/Storage
DATA_DIR=./data

# Docker Configuration
COMPOSE_PROJECT_NAME=zakapp

# Build Configuration
BUILD_MODE=development

# =====================================================
# PORT CONFIGURATION GUIDE
# =====================================================
# The application uses two ports:
# 1. Backend API Server: Default 3001 (configure in server/.env)
# 2. Frontend Dev Server: Default 3000 (configure in client/.env.local)
#
# To change ports:
# 1. Backend: Set PORT in server/.env
# 2. Frontend: Set PORT in client/.env.local
# 3. Update REACT_APP_API_BASE_URL in client/.env.local to match backend port
#
# Example for custom ports:
# - Backend on 3081: server/.env → PORT=3081
# - Frontend on 3010: client/.env.local → PORT=3010
# - Connect them: client/.env.local → REACT_APP_API_BASE_URL=http://localhost:3081/api
#
# - client/.env.example
# - DEVELOPMENT_SETUP.md

# =====================================================
# PORT CONFIGURATION GUIDE
# =====================================================
# ZakApp uses configurable ports for all services.
# Each service has two port settings:
#   - External Port: What you access from your browser/host
#   - Internal Port: What the service listens on inside Docker
#
# Frontend Server
FRONTEND_PORT=3000              # External: http://localhost:3000
FRONTEND_INTERNAL_PORT=3000     # Internal: Vite dev server port
#
# Backend API Server
BACKEND_PORT=3001               # External: http://localhost:3001
BACKEND_INTERNAL_PORT=3001      # Internal: Express server port
#
# CouchDB Database
COUCHDB_PORT=5984               # External: http://localhost:5984
COUCHDB_INTERNAL_PORT=5984      # Internal: CouchDB listen port
#
# Example: Cloudflare Tunnel deployment
# FRONTEND_PORT=8080
# BACKEND_PORT=8081
# COUCHDB_PORT=8082
# (Keep internal ports default for simplicity)
#
# CORS Configuration
# ALLOWED_ORIGINS=https://app.example.com,https://api.example.com
# COUCHDB_ALLOWED_ORIGINS=https://app.example.com  # Optional override if different from ALLOWED_ORIGINS
#
# See docs/PORT_CONFIGURATION.md for detailed examples
# =====================================================

# CouchDB Configuration
# COUCHDB_USER and COUCHDB_PASSWORD are defined in secrets/ if using docker-compose.prod.yml
# For development:
COUCHDB_USER=admin
COUCHDB_PASSWORD=password