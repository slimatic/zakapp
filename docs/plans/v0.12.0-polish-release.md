# ZakApp v0.12.0 Polish Release — Implementation Plan

> For Hermes: Use executing-plans / dispatching-parallel-agents. Work on branch `feature/v0.12.0-polish`.

**Goal:** Ship v0.12.0 as a trust + polish release after v0.11.0. Fix CI, cleanup dead code, resolve small TODOs, add two user-facing improvements, and leave the repo release-ready.

**Target version:** `0.12.0`

**Branch:** `feature/v0.12.0-polish`

**Scope:** Option B from v0.11.1/v0.12.0 audit.

---

## Release Streams

### Stream 1: Infrastructure & Trust (P0)

| Task | What | How to verify |
|---|---|---|
| S1.1 | Fix `server/package.json` license to `AGPL-3.0-or-later` | `grep license server/package.json` |
| S1.2 | Remove committed artifacts from git | `git ls-files` no longer lists `prod.db.backup-*`, `*-test-results.json`, `delete-test-result.json`, `server/test-results.json` |
| S1.3 | Add missing patterns to `.gitignore` | `git status --short` after `git rm --cached` shows clean |
| S1.4 | Re-enable server tests in CI | `.github/workflows/test.yml` uncomments server test step; CI passes |
| S1.5 | Fix CLI deps (`commander`, `inquirer`, `@zakapp/shared`) and TS errors | `cd cli && npm test` passes; `npx tsc --noEmit` passes |
| S1.6 | Bump version to `0.12.0` in root/server/client/shared/cli package.json | All four files report `0.12.0` |

### Stream 2: Dead Code Cleanup (P1)

| Task | What | How to verify |
|---|---|---|
| S2.1 | Delete wrapper middleware `server/src/middleware/auth.ts`, `security.ts`, `validation.ts` if unused | `grep -r "from '.*middleware/auth'" server/src` returns nothing; tests still pass |
| S2.2 | Delete orphaned `server/models/*.js` and `server/routes/*.js` if unused | Same pattern; build passes |
| S2.3 | Remove tracked `client/dist/` from git and ensure `.gitignore` covers it | `git ls-files client/dist` returns empty |
| S2.4 | Archive or delete stale local branches `feature/muharram-audit-cleanup`, `fix/muharram-release` | `git branch` no longer lists them (after user confirms) |

### Stream 3: TODO / FIXME Resolution (P1)

| Task | What | How to verify |
|---|---|---|
| S3.1 | Categorize 38 TODO/FIXME/TEMP markers | Spreadsheet/list in this plan |
| S3.2 | Resolve quick wins (typos, dead references, low-risk) | Tests pass |
| S3.3 | Convert real feature gaps into GitHub issues | Issues created; markers replaced with `TODO(#issue)` |
| S3.4 | Remove stale `TEMPORARILY DISABLED` imports in `ZakatService.ts` or wire them back | `npx tsc --noEmit` clean; tests pass |

### Stream 4: User-Facing Features (P1)

| Task | What | How to verify |
|---|---|---|
| S4.1 | Dashboard Action Cards — replace "Smart Navigation" on empty dashboard | New component `DashboardActionCards.tsx`; visible when no assets/nisab records; unit tests |
| S4.2 | Offline caching strategy — add service worker / Workbox config for static assets | Lighthouse PWA audit score improves; `workbox` or CRA PWA template integrated; manual offline smoke test |

### Stream 5: Quality Sweep (P2)

| Task | What | How to verify |
|---|---|---|
| S5.1 | ESLint unused-import sweep across client/server/shared/cli | `npm run lint` passes (or at least no unused-import errors) |
| S5.2 | Review and merge 2 open Dependabot PRs if safe | `gh pr merge --squash` after CI passes |
| S5.3 | Update CHANGELOG with v0.12.0 entry | CHANGELOG.md has 0.12.0 section |

---

## Detailed Tasks

### S1.1 Fix server license

**Files:** `server/package.json:91`

**Change:**
```json
"license": "AGPL-3.0-or-later",
```

**Verify:** `grep '"license"' server/package.json` prints the AGPL value.

---

### S1.2 Remove committed artifacts

**Files to remove from git:**
- `prod.db.backup-pre-ralph-20260208-143238`
- `auth-test-results.json`
- `delete-test-result.json`
- `payment-test-results.json`
- `payment-test-results-2.json`
- `server/test-results.json`

**Command:**
```bash
git rm --cached prod.db.backup-pre-ralph-20260208-143238 \
  auth-test-results.json delete-test-result.json \
  payment-test-results.json payment-test-results-2.json \
  server/test-results.json
```

**Verify:** `git ls-files | grep -E 'prod\.db\.backup|test-results\.json$|delete-test-result\.json$|payment-test-results.*\.json$'` returns nothing.

---

### S1.3 Harden `.gitignore`

**File:** `.gitignore`

**Add:**
```gitignore
# Test / backup artifacts
*test-results*.json
delete-test-result.json
payment-test-results*.json
auth-test-results.json
*.backup-*
*.db.backup-*
backups/*.db.backup-*
```

**Verify:** After `git rm --cached`, `git status --short` shows only staged deletions for the artifacts, no untracked noise.

---

### S1.4 Re-enable server tests in CI

**File:** `.github/workflows/test.yml`

**Change:** Uncomment server test step and make it run before client build for faster failure.

```yaml
    - name: Run server tests
      run: cd server && npm test
      env:
        CI: true
        NODE_ENV: test
        ENCRYPTION_KEY: test-key-32-bytes-long-for-ci-only
        JWT_SECRET: test-jwt-secret-for-ci-only
        DATABASE_URL: file:./test.db
```

**Verify:** Open a draft PR; server tests must pass in CI.

---

### S1.5 Fix CLI deps and TS errors

**File:** `cli/package.json`

**Check:** The dependencies list already has `commander` and `inquirer`, but `node_modules` may be stale. Run `cd cli && npm install`.

**If tests still fail with missing packages**, run from root:
```bash
cd cli && npm install --legacy-peer-deps
```

**Check `@zakapp/shared` symlink:** `ls -la cli/node_modules/@zakapp/shared`. If missing:
```bash
cd cli && npm install ../shared --legacy-peer-deps
```

**TypeScript errors:** The TS errors about `any` params and `unknown` come from missing types due to uninstalled deps. After deps install, run `cd cli && npx tsc --noEmit`. If still failing, fix explicit types per compiler output.

**Verify:** `cd cli && npm test` passes (4/4 test files).

---

### S1.6 Version bump

**Files:** `package.json`, `server/package.json`, `client/package.json`, `shared/package.json`, `cli/package.json`

**Change:** `"version": "0.12.0"` in all five.

**Verify:** `grep -n '"version"' package.json server/package.json client/package.json shared/package.json cli/package.json` all show `0.12.0`.

---

### S2.1 Delete wrapper middleware

**Files to inspect:**
- `server/src/middleware/auth.ts`
- `server/src/middleware/security.ts`
- `server/src/middleware/validation.ts`

**Approach:**
1. Read each file.
2. Search for imports of each: `grep -r "middleware/auth" server/src` etc.
3. If unused and just re-exports, delete.
4. If used, decide whether to update imports to PascalCase impl or keep as compatibility re-export.

**Verify:** `cd server && npm test` still passes after deletion.

---

### S2.2 Delete orphaned JS files

**Files to inspect:**
- `server/models/index.js`
- `server/routes/assets.js`
- `server/routes/auth.js`
- `server/routes/user.js`
- `server/routes/zakat.js`
- `server/utils/dataStore.js`
- `server/utils/zakatCalculator.js`

**Approach:** Same as S2.1 — check imports, delete if orphaned.

**Verify:** Server build and tests pass.

---

### S2.3 Untrack `client/dist`

**Command:**
```bash
git rm -r --cached client/dist
```

**Verify:** `git ls-files client/dist | head` returns nothing.

---

### S3.1–S3.4 TODO resolution

**Known TODOs from audit (38 total):**

Quick wins / cleanup:
- `client/src/App.tsx:232` — comment about hidden History. Create issue or remove comment.
- `client/src/__tests__/unit/calendarConverter.test.ts:210,261` — uncomment tests if functions exist; otherwise create issues.
- `shared/src/constants.ts:19,27` — already marked DEPRECATED. Remove file if nothing imports it.

Real feature gaps — create GitHub issues:
- `server/src/routes/analytics.ts:307` — analytics dashboard persistence
- `server/src/controllers/AuthController.ts:468` — email reset token
- `server/src/controllers/AuthController.ts:334,345,576` — session/token invalidation
- `server/src/services/PushNotificationService.ts:112,199` — push notifications
- `server/src/services/AnnualSummaryService.ts:154-210` — recipient counting / payment math
- `server/src/services/NisabService.ts:438` — historical price data
- `server/src/services/hawlTrackingService.ts:398,436` — interruption tracking

TEMP imports:
- `server/src/services/ZakatService.ts:20-22,147,158` — remove disabled imports or decide if engine still needed.

**Verify:** After cleanup, `grep -rn "TODO\|FIXME\|HACK\|XXX\|TEMP\|DEPRECATED" server/src client/src shared/src cli/src | wc -l` is reduced.

---

### S4.1 Dashboard Action Cards

**Goal:** Replace the current "Smart Navigation" on an empty dashboard with simple "Next Best Action" cards (e.g., "Add Assets", "Create Nisab Year Record", "Add Payment").

**Files:**
- Create: `client/src/components/dashboard/DashboardActionCards.tsx`
- Modify: `client/src/pages/Dashboard.tsx` (replace Smart Navigation when empty)
- Test: `client/src/components/dashboard/__tests__/DashboardActionCards.test.tsx`

**Behavior:**
- If user has no assets → show "Add Assets" card linking to asset creation.
- If no active nisab year record → show "Start Nisab Year" card.
- If owed zakat > paid → show "Make Payment" card.
- Otherwise → show analytics / summary cards.

**Verify:** `cd client && npm test -- --run DashboardActionCards` passes. Manual browser check optional.

---

### S4.2 Offline caching strategy

**Goal:** Add a service worker / Workbox to cache static assets so the app works offline after first load.

**Files:**
- `client/src/service-worker.ts` (if exists)
- `client/src/index.tsx`
- `client/package.json`

**Approach:**
1. Check current CRA/PWA setup: `grep -r "serviceWorker" client/src`.
2. If CRA template was ejected, re-register worker.
3. If no worker exists, create minimal `service-worker.ts` using Workbox to cache app shell and API responses (where safe).
4. Ensure offline test: build client, serve `client/build`, open in browser, disable network, reload.

**Verify:** Lighthouse PWA audit shows "Works offline" green; manual reload with network off shows dashboard.

---

### S5.1 Unused import sweep

**Command:**
```bash
cd client && npm run lint -- --fix 2>&1 | tail -20
cd server && npm run lint -- --fix 2>&1 | tail -20
```

If no lint script exists, run ESLint directly or skip if project uses CRA's built-in.

**Verify:** No new lint errors introduced; build passes.

---

### S5.2 Dependabot PRs

**PRs:**
- #305 nodemailer 8→9 in `/cli`
- #304 npm_and_yarn group across 3 directories

**Approach:**
1. Check CI status with `gh pr view 305 --json statusCheckRollup`.
2. If green, `gh pr merge 305 --squash --delete-branch`.
3. Repeat for #304 if safe.
4. If either has breaking changes or red CI, leave open and note in release report.

---

### S5.3 CHANGELOG update

**File:** `CHANGELOG.md`

**Add v0.12.0 section at top:**
```markdown
## [0.12.0] - 2026-07-XX

### Trust & CI
- Fixed `server/package.json` license to AGPL-3.0-or-later
- Removed committed test result artifacts and DB backup from git
- Re-enabled server tests in GitHub Actions CI
- Fixed CLI dependency installation and TypeScript errors

### Cleanup
- Removed unused wrapper middleware and orphaned JS route/model files
- Removed tracked `client/dist/` build output
- Resolved or ticketed 38 TODO/FIXME markers

### Features
- Dashboard Action Cards: "Next Best Action" prompts on empty dashboard
- Offline caching: service worker caches static assets for offline use

### Quality
- ESLint unused-import sweep across client/server/shared/cli
- Dependency updates via Dependabot
```

---

## Verification Gates

Before tagging v0.12.0, all of these must pass:

- [ ] `cd server && npm test` passes
- [ ] `cd client && npm test -- --run` passes (or same skip count as baseline)
- [ ] `cd cli && npm test` passes
- [ ] `cd server && npx tsc --noEmit` passes
- [ ] `cd client && npx tsc --noEmit` passes
- [ ] `cd cli && npx tsc --noEmit` passes
- [ ] `cd client && npm run build` passes
- [ ] `cd server && npm run build` passes
- [ ] No committed artifacts in `git ls-files`
- [ ] All package.json versions = `0.12.0`
- [ ] CI workflow runs server + client tests
- [ ] CHANGELOG has v0.12.0 entry

---

## Release Sequence

1. Finish all streams on `feature/v0.12.0-polish`.
2. Push branch: `git push -u origin feature/v0.12.0-polish`.
3. Open PR with `gh pr create --fill`.
4. Wait for CI green.
5. Merge with squash.
6. On `main`: `git tag v0.12.0 && git push origin v0.12.0`.
7. (Optional) Trigger Docker Hub build if `docker-hub.yml` runs on tags.
8. Report results.
