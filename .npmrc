# Fix npm permission issues
progress=false
loglevel=warn

# Use exact versions to avoid unexpected updates
save-exact=true

# Disable package-lock
package-lock=false

# Use cache more efficiently (prefer-offline replaces deprecated cache-min)
prefer-offline=true

# Security settings
audit-level=moderate