# Frontend Environment Configuration
# Copy this file to .env.local for local development

# =====================================================
# BACKEND API CONFIGURATION (REQUIRED)
# =====================================================
# The base URL for the backend API server
# Format: http://localhost:PORT/api
# 
# IMPORTANT: This must match your backend server PORT setting!
# - If backend PORT=3001, use: http://localhost:3001/api
# - If backend PORT=3081, use: http://localhost:3081/api
# - For production: use your domain (e.g., https://api.yourdomain.com/api)
REACT_APP_API_BASE_URL=http://localhost:3001/api

# Alternative: Set server URL without /api suffix
# REACT_APP_SERVER_URL=http://localhost:3001

# =====================================================
# FRONTEND PORT CONFIGURATION (OPTIONAL)
# =====================================================
# The port the React development server will run on
# Default: 3000
# To change: Set PORT in your shell or .env file
# Example: PORT=3010 npm start
PORT=3000

# =====================================================
# APPLICATION CONFIGURATION
# =====================================================
# Application title shown in browser tab
REACT_APP_APP_TITLE=ZakApp - Zakat Calculator

# Environment name (development, staging, production)
REACT_APP_ENV=development

# =====================================================
# FEATURE FLAGS
# =====================================================
# Enable/disable analytics tracking
REACT_APP_ENABLE_ANALYTICS=false

# Enable/disable debug mode and console logs
REACT_APP_ENABLE_DEBUG=true

# Enable/disable feedback widget for beta testing
# Set to 'true' to show feedback bubble, 'false' to hide
REACT_APP_FEEDBACK_ENABLED=true

# =====================================================
# QUICK START GUIDE
# =====================================================
# 1. Copy this file: cp .env.example .env.local
# 2. Update REACT_APP_API_BASE_URL to match your backend port
# 3. (Optional) Change PORT to use a different frontend port
# 4. Start development: npm start
#
# TROUBLESHOOTING:
# - "Failed to fetch" errors? Check REACT_APP_API_BASE_URL matches backend PORT
# - Port already in use? Change PORT to a different number (e.g., 3010)
# - Backend not responding? Ensure backend server is running on the specified port
